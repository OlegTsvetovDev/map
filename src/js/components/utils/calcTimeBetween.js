import { steps } from '../settings/settings.js'


const calcTimeBetween = (currTime, nextTime) => {
  // const timeBetween = Math.floor((Date.parse(nextTime) - Date.parse(currTime)) / 60000)

  // return timeBetween || 60
  return 60
}


export default calcTimeBetween
