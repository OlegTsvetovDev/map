// шаблон метки транспорта для рендера
const iconTransportTemplate = route => {
  return `
    <div class="icon__content">
      <p>${route}</p>
    </div>
  `
}


export default iconTransportTemplate
